# JYXR Web 编辑器模块化与标准化重构计划

> 文档状态：执行中（Living Plan）
> 适用目录：`tools/JsonEditor`
> 建立日期：2026-07-11
> 当前阶段：阶段 4——角色与伙伴工作区（已完成，用户已验证）；下一阶段为阶段 5——资源管理器与统一资源选择器
> 核心目标：在不修改游戏底层模型和运行规则的前提下，把现有 JSON 工具重构为面向 MOD 新人的模块化、标准化内容编辑器。

---

## 1. 为什么需要这次重构

当前编辑器已经具备 MOD 选择、JSON 编辑、部分专用表单、剧情 DSL、剧情图、资源预览、静态检查和内容校验等能力，但这些能力是逐步叠加形成的，尚未形成统一的产品结构。

当前主要问题不是单纯的视觉样式，而是：

1. 编辑器以文件和 JSON 结构为主要入口，而新人以“创建角色、添加地图点位、编写剧情”等任务理解 MOD 制作。
2. 全局操作、当前文件操作、当前对象操作、检查工具和创作工具同时堆在顶部。
3. 页面采用固定外部三栏，中间专用表单又继续分栏，地图和剧情工作区的有效空间不足。
4. “新增 / 新建 / 添加”“检查 / 校验”等术语不统一。
5. 普通操作、主操作和危险操作缺乏稳定的视觉层级。
6. 角色、地图、物品、商店、剧情和资源工具分别实现相似控件，重复逻辑逐渐增加。
7. `wwwroot/app.js` 与 `wwwroot/styles.css` 已成为大型单文件，功能边界和样式作用域不清晰。
8. 帮助内容偏向参考手册，缺少面向新人的任务式引导、字段说明和问题修复建议。

本次重构不是换皮，也不是另写一套与游戏脱节的数据模型，而是重新建立编辑器自己的信息架构、交互规范和前端模块边界。

---

## 2. 不可突破的边界

### 2.1 绝对原则

1. 不修改游戏底层代码来迁就编辑器。
2. 不发明游戏运行时不支持的字段、条件、事件或资源语义。
3. 不改变现有 MOD JSON 字段的含义。
4. 不把编辑器专用状态写入正式 MOD JSON。
5. 不修改用户 MOD 数据作为重构手段。
6. 不把高级 JSON 能力删除；高级能力应被收纳，而不是被阉割。
7. 不进行一次性“大爆炸”重写；每个阶段必须可独立验证和回退。
8. 保持本地优先、显式、可检查和零依赖优先。

### 2.2 默认禁止修改的目录

除非用户在后续单独明确授权，本计划不修改：

```text
src/Game.Core
src/Game.Application
src/Game.Content
src/Game.Godot
scenes
assets
mods
launcher
autoload
```

### 2.3 本次重构允许修改的主要范围

```text
tools/JsonEditor/Program.cs
tools/JsonEditor/README.md
tools/JsonEditor/DEVELOPMENT.md
tools/JsonEditor/REFACTORING_PLAN.md
tools/JsonEditor/wwwroot/**
```

其中：

- `Program.cs` 只允许为编辑器增加查询、资源索引、检查、摘要或保存接口；不得承载游戏业务规则的替代实现。
- `wwwroot` 负责页面壳层、工作区、表单、交互组件和前端状态。
- 正式数据规则必须以已有游戏定义和当前内容加载约束为准。

---

## 3. 验证约定

### 3.1 Codex 执行约定

用户已明确要求 Codex 不运行构建。因此每个阶段由 Codex执行的验证仅包括静态检查，例如：

- 检查 Git diff 和修改范围。
- 检查 JavaScript 语法（`node --check`，适用于普通脚本；ES Module 使用对应静态导入检查方式）。
- 检查 HTML 中 ID、模块引用和资源路径的一致性。
- 检查 CSS 选择器、设计令牌和明显的重复定义。
- 检查前端调用的 API 路径是否与 `Program.cs` 暴露端点一致。
- 搜索被移除 DOM ID、旧函数名和遗留样式引用。
- 检查不应修改的目录是否发生变化。
- 审阅数据读写路径，确认没有改变 MOD JSON 语义。

Codex 不执行：

```text
dotnet build
dotnet run
dotnet test
启动 JsonEditor 服务
启动 Godot
浏览器运行时自动化测试
```

除非用户之后明确改变这一约定。

### 3.2 用户手动验证约定

每个实施阶段完成后，Codex 必须给出与该阶段匹配的手动验证步骤。用户可自行启动编辑器：

```bash
cd /Users/zheng/jyxr-web-editor
dotnet run --project tools/JsonEditor/JsonEditor.csproj
```

然后访问：

```text
http://localhost:5127
```

每个阶段的汇报必须明确区分：

- Codex 已静态确认的内容。
- 因未运行服务而尚未运行时确认的内容。
- 用户需要手动点击验证的内容。
- 发现问题后应反馈的页面、步骤和截图位置。

---

## 4. 产品定位与目标用户

### 4.1 第一目标用户

没有前端或 C# 经验、不熟悉项目文件结构，但希望完成以下工作的 MOD 制作者：

- 创建或修改角色。
- 选择头像、图标、背景、音乐等已有资源。
- 创建地图和地图交互点。
- 为地图点位绑定剧情、商店、储物箱、战斗或地图跳转。
- 创建物品、装备和商店商品。
- 编写剧情、对话、选项和已有剧情命令。
- 检查资源缺失、ID 重复和引用错误。
- 保存后进入游戏手动验证。

### 4.2 第二目标用户

熟悉 JSON、Story DSL 和游戏数据结构的高级制作者。编辑器必须保留：

- 原始 JSON 编辑。
- Story DSL 编辑。
- 技术 ID。
- 原始引用值。
- 格式化。
- 文件级搜索。
- 高级条件和未识别字段查看。

### 4.3 核心体验目标

编辑器默认回答四个问题：

1. 我现在正在编辑哪个 MOD？
2. 我现在正在制作什么内容？
3. 当前修改是否已经保存，是否存在问题？
4. 下一步应该做什么？

---

## 5. 设计原则

### 5.1 创作任务优先，技术文件退后

默认导航使用“角色与伙伴”“地图与事件”“剧情与任务”等创作语言。具体 JSON 文件放入高级数据入口或模块内部说明。

### 5.2 一个页面只有一个明确主操作

例如：

- 角色页面：`新建角色`
- 地图页面：`新建地图`
- 剧情页面：`新建剧情`
- 资源页面：根据现有能力使用 `注册资源` 或明确的导入动作

同一页面不能同时出现多个视觉权重相同的主按钮。

### 5.3 全局操作与上下文操作分离

全局顶栏只保留：

- 当前 MOD。
- 全局搜索或命令入口。
- 问题数量。
- 未保存状态。
- 保存。

文件格式化、地图工具、剧情工具、资源检查等回到所属工作区。

### 5.4 默认新手模式，高级能力按需展开

新手默认看到：

- 中文字段名。
- 必填标记。
- 简短说明。
- 默认值和示例。
- 枚举、引用和资源选择器。
- 即时问题提示。

高级模式显示：

- 原始 JSON / DSL。
- 技术 ID。
- 原始条件结构。
- 未识别字段。
- 格式化和技术诊断。

两种模式编辑同一份内存数据，不建立两套业务模型。

### 5.5 主工作区优先

右侧检查器不永久占位，改为上下文抽屉。左侧主导航可折叠，模块内列表属于主工作区。地图画布和剧情画布必须获得足够宽度。

### 5.6 数据操作可预期

用户需要明确知道：

- 修改对象是谁。
- 是否尚未保存。
- 删除是否被其他内容引用。
- 错误原因和修复建议。
- 保存后下一步如何在游戏中验证。

### 5.7 统一而非重复

按钮、对话框、字段、标签页、对象列表、资源选择器、引用选择器、问题列表和空状态应形成共享组件，不允许各工作区继续独立复制一套。

### 5.8 逐步迁移，不破坏现有能力

新壳层和工作区迁移期间，未迁移的数据类型继续通过高级 JSON 工作区使用。迁移完成一个模块后，再删除该模块对应的遗留入口和样式。

---

## 6. 目标信息架构

```text
项目首页

内容创作
├── 剧情与任务
├── 地图与事件
├── 角色与伙伴
├── 物品与装备
├── 武学与能力
├── 商店与经济
├── 战斗与挑战
└── 世界规则

资源管理
├── 图片与头像
├── 图标与背景
├── 音频
├── 动画
└── 资源注册与引用

质量检查
├── 全部问题
├── 数据问题
├── 引用问题
├── 资源问题
└── 剧情连接问题

高级数据
├── JSON 文件
├── Story 源文件
├── 游戏配置
└── 技术工具
```

说明：

- 底层仍然读取当前 MOD 的 `data` 目录。
- 信息架构只改变“用户如何进入功能”，不改变数据保存位置。
- 尚未专用化的数据类型先映射到“高级数据”。
- 模块名称不代表新增运行时概念，只是对现有文件的用户友好分组。

---

## 7. 目标页面壳层

```text
┌───────────────────────────────────────────────────────────────┐
│ 当前 MOD    全局搜索              问题    未保存状态    保存  │
├──────────────┬────────────────────────────────────────────────┤
│ 主导航       │ 面包屑                                         │
│              │ 页面标题、说明、当前页面唯一主操作             │
│ 项目首页     ├────────────────────────────────────────────────┤
│ 内容创作     │                                                │
│ 资源管理     │ 主工作区                                       │
│ 质量检查     │                                                │
│ 高级数据     │ 列表 + 详情 / 专用画布 / 高级编辑器            │
│              │                                                │
├──────────────┴────────────────────────────────────────────────┤
│ 保存、检查、选择状态                                          │
└───────────────────────────────────────────────────────────────┘
```

按需打开的右侧上下文抽屉：

```text
字段帮助 / 当前问题 / 资源预览 / 引用关系 / 高级属性
```

### 7.1 响应式目标

至少建立三档，而不是仅依赖一个小屏断点：

- 宽屏：主导航 + 主工作区 + 可选抽屉。
- 中屏：主导航可折叠，抽屉覆盖或压缩显示。
- 窄屏：主导航使用临时抽屉，列表与详情改为前后页面或纵向布局。

第一轮重点保证桌面浏览器和常见笔记本宽度；移动端不作为主要创作设备，但不能完全不可用。

---

## 8. 标准组件体系

### 8.1 设计令牌

建立统一 CSS 变量：

- 颜色：背景、表面、边框、文本、弱文本、主色、成功、警告、危险。
- 间距：4 / 8 / 12 / 16 / 24 / 32。
- 圆角：小控件、卡片、弹窗。
- 阴影：浮层、抽屉、弹窗。
- 字号：辅助、正文、小标题、页面标题。
- 控件高度：紧凑、标准、大型。
- Z 轴：顶栏、抽屉、菜单、弹窗、提示。

不允许工作区随意声明新的近似颜色、边距和按钮高度。

### 8.2 按钮

统一类型：

- `primary`：页面唯一主操作或确认动作。
- `secondary`：普通明确动作。
- `ghost`：低优先级动作。
- `danger`：删除、清空、覆盖、丢弃。
- `icon`：折叠、排序、关闭等紧凑动作。
- `menu` / `split`：多个低频操作。

统一术语：

| 使用场景 | 统一文案 |
|---|---|
| 创建独立对象 | 新建 |
| 向集合增加子项 | 添加 |
| 选择已有对象或资源 | 选择 |
| 创建当前对象副本 | 复制 |
| 移除对象 | 删除 |
| 检查数据、引用和资源 | 检查 |
| 整理 JSON 文本 | 格式化 |
| 写入文件 | 保存 |

### 8.3 字段

统一字段结构：

```text
标签 + 必填标记
说明或示例
输入控件
字段级问题
可选高级信息
```

共享字段组件至少覆盖：

- 文本。
- 多行文本。
- 数字。
- 布尔值。
- 枚举。
- ID。
- 内容引用。
- 资源引用。
- JSON 高级字段。
- 数组子项。

### 8.4 对象列表

统一支持：

- 搜索。
- 类型或状态筛选。
- 当前选中状态。
- 未保存标记。
- 错误/警告徽标。
- 空状态。
- 新建主操作。
- 更多菜单中的复制和删除。

### 8.5 对话框和抽屉

统一处理：

- 新建对象。
- 删除确认。
- 未保存离开确认。
- 资源选择。
- 引用选择。
- 问题详情。
- 高级属性。

不得继续使用浏览器原生 `alert/confirm/prompt` 作为主要交互方案；迁移阶段可临时保留，但必须登记清理。

### 8.6 问题展示

统一等级：

- 错误：可能导致无法解析、无法保存或关键引用无效。
- 警告：可以保存，但结果可能不符合预期。
- 建议：不影响功能的完善建议。

每条问题至少包含：

- 等级。
- 对象类型。
- 对象名称或 ID。
- 字段。
- 原因。
- 修复建议。
- 定位动作。

### 8.7 专用工作区迁移强制规范

阶段 4角色工作区的手动验证暴露出一组可复用规则。阶段 5之后的资源、地图、物品、装备、商店和剧情工作区必须默认遵守，不允许等用户逐项反馈后再补。

#### 数据与保存

- 专用表单、高级 JSON和隐藏文本编辑器必须编辑同一份内存对象，不建立互相复制的第二份业务状态。
- 未识别字段必须保留；结构化控件只修改自己拥有的字段。
- 保存继续写回现有正式 JSON文件，不新增编辑器专用游戏字段。
- 保存、重新读取、切换 MOD和丢弃修改后必须回到当前新版工作区，禁止旧表单因通用刷新逻辑重新显示。
- 新版工作区建立后，旧表单只允许作为尚未迁移模块的临时实现；对应模式必须显式隔离旧 DOM。旧代码统一在阶段 12删除。

#### 搜索与输入

- 所有 `type=search` 和会触发列表/弹层重绘的实时输入必须使用 `core/input-composition.js`，不得直接在 `input` 事件中重建输入框。
- 中文输入法以 composition 完成后的最终值触发筛选；尾随同值事件按值去重，不得通过“跳过下一次事件”实现。
- 搜索至少覆盖中文显示名和稳定 ID；内容量较大时增加描述、类型和关键属性，并支持包含匹配与顺序模糊匹配。
- 搜索结果必须支持键盘上下选择、回车确认、Esc关闭，并限制一次渲染数量。
- 重绘后需要继续输入的搜索框必须恢复焦点和光标位置。

#### 内容引用

- 普通用户不应手写引用 ID；模型、定义、技能、装备、资源等引用优先使用共享 `reference-picker.js`。
- 引用选项使用编辑器只读投影，例如 `{ id, name, type, iconPath, subtitle, description, metadata, searchText }`，投影字段不得写入正式 JSON。
- 控件展示“名称（ID）”，正式 JSON只保存稳定 ID和游戏支持的实例状态。
- 已选项与搜索结果应显示可用图标、描述和领域关键属性；缺图或引用失效必须有明确占位和状态。
- 未知旧引用必须保留并标记“当前引用不存在”，不能因为打开表单而静默删除。
- 数组引用必须提供搜索添加、已选列表、删除和去重；带实例状态的引用还要提供等级、上限、启用/装备等结构化字段。

#### 资源注册与修复

- “选择已有资源”“注册新资源”“修正已有资源”是三种不同动作，界面和后端必须明确区分。
- 写入前必须展示稳定 ID、资源组、value、真实资产路径和将执行的创建/复用/修正动作，并进行冲突预检。
- 同 ID同路径可以复用；同 ID不同路径不得静默覆盖；资源组冲突必须阻断。
- 受控修正保持稳定 ID不变，只更新允许修改的资源字段；请求携带期望旧值，避免覆盖操作期间产生的新状态。
- 所有直接写文件的编辑器 API必须先完成全部预检，再备份，再写入，避免部分成功。
- 创建或修正完成后必须提供定位到资源定义的入口；跨文件前先处理当前未保存状态。
- 资源 ID遵循当前项目的人类可检索规则，例如头像使用 `头像.中文名`；技术文件路径可以使用独立的英文或拼音路径。

#### 图片处理

- 编辑器必须显示真实文件、解析 value、像素尺寸和注册状态，不能只显示预览图。
- 图片尺寸规范是建议而非游戏规则；非推荐尺寸只警告，不阻止用户注册或使用。
- 可选规范化必须明确说明操作，保留宽高比，不裁剪主体、不替换背景、不做 AI补图；需要补齐画布时使用透明区域。
- 覆盖资产前必须备份原图。当前自动规范化只处理受支持目录中的 PNG，其他格式明确提示但不阻断使用。
- 编辑器只校验文件存在且元数据可读取，不修改游戏运行时的缩放、裁切或显示逻辑。

#### 创建、复制、删除与问题定位

- “新建完整对象”和“注册轻量对象/资源”必须分开命名，不能把不同写入语义混为一个按钮。
- 新建使用游戏支持的安全最小结构；复制执行深复制并生成唯一 ID；操作后保持未保存状态，除非明确标记为直接写盘工具。
- 删除前展示静态引用位置，并明确静态检查不能覆盖动态脚本和运行时引用。
- 字段问题、资源问题和引用问题必须能从问题中心定位回工作区、具体对象和可识别字段。
- 错误、警告和建议必须按语义区分：可继续使用但存在表现风险的情况不得错误地实现成阻断。

#### 后续工作区验收清单

每个专用工作区宣布完成前，至少验证：

1. 中文搜索和连续输入不会丢字、失焦或打断输入法。
2. 常用引用不需要手写 ID，名称、ID、图标、描述和关键属性可见。
3. 表单与高级 JSON双向一致，未知字段保留。
4. 保存后仍停留在新版工作区，不出现旧页面。
5. 新建、复制、删除、直接注册和资源修正的写入语义清楚。
6. 重名、同 ID异路径、缺失引用和失效资源均有明确处理。
7. 未保存离开、跨文件定位和切换 MOD不会静默丢数据。
8. 资源处理只修改编辑器范围内允许修改的文件，不触碰游戏底层代码、Godot场景或运行时规则。
9. 完成静态语法、模块导入、DOM ID、CSS结构、API路径、缓存版本、`git diff --check`和修改范围检查。
10. 提供与当前工作区匹配的用户手动验证步骤，并在用户确认后才标记阶段完成。

---

## 9. 目标前端模块结构

第一阶段保持原生 JavaScript 和零构建前端，逐步切换到浏览器原生 ES Modules：

```text
wwwroot/
├── index.html
├── app.js                        # 迁移期入口，最终仅负责启动
├── core/
│   ├── state.js                  # 全局应用态及可观察变更
│   ├── api.js                    # 所有 HTTP 请求
│   ├── router.js                 # 工作区和对象定位
│   ├── events.js                 # 前端事件总线或明确事件分发
│   ├── commands.js               # 保存、搜索、打开问题等全局命令
│   ├── dirty-state.js            # 未保存状态
│   └── preferences.js            # 纯编辑器布局偏好
├── ui/
│   ├── shell.js                  # 顶栏、主导航、状态栏、抽屉容器
│   ├── buttons.js
│   ├── fields.js
│   ├── lists.js
│   ├── tabs.js
│   ├── menus.js
│   ├── dialogs.js
│   ├── drawers.js
│   ├── empty-state.js
│   ├── resource-picker.js
│   ├── reference-picker.js
│   └── problem-list.js
├── workspaces/
│   ├── dashboard.js
│   ├── characters.js
│   ├── maps.js
│   ├── items.js
│   ├── shops.js
│   ├── stories.js
│   ├── resources.js
│   ├── problems.js
│   └── advanced-data.js
├── domain/
│   ├── content-index.js          # 编辑器只读索引，不替代游戏仓储
│   ├── labels.js                 # 类型和字段中文显示
│   ├── editor-validation.js      # 编辑器即时提示，不能替代后端正式校验
│   └── defaults.js               # 仅使用运行时支持的安全默认结构
└── styles/
    ├── tokens.css
    ├── base.css
    ├── shell.css
    ├── components.css
    ├── forms.css
    ├── editor.css
    └── workspaces.css
```

### 9.1 模块边界规则

1. 工作区不直接散落 `fetch`，统一通过 `core/api.js`。
2. 工作区不自行创建不同样式按钮，统一通过共享组件或标准 class。
3. 工作区不直接操作全局顶栏 DOM，通过 shell/commands 更新。
4. 资源 ID 解析、预览和选择统一收敛，不在角色、物品、地图和剧情中分别复制。
5. 前端即时检查只负责帮助用户，不取代后端正式内容校验。
6. 保存格式继续以当前后端和已有文件格式为准。
7. 原始 JSON 编辑器在迁移期继续作为最终兜底能力。

### 9.2 后端模块化方向

`Program.cs` 当前同时包含端点注册和大量编辑器辅助逻辑。待前端壳层稳定后，再按实际需要拆分：

```text
JsonEditor/
├── Program.cs
├── Hosting/
│   ├── WorkspaceEndpoints.cs
│   ├── ContentEndpoints.cs
│   ├── StoryEndpoints.cs
│   ├── AssetEndpoints.cs
│   └── StaticToolEndpoints.cs
├── Workspace/
│   ├── EditorWorkspace.cs
│   └── ModWorkspace.cs
├── Services/
│   ├── EditorContentIndex.cs
│   ├── EditorValidationService.cs
│   ├── AssetCatalogService.cs
│   └── BackupService.cs
└── Contracts/
    ├── Requests.cs
    └── Responses.cs
```

后端拆分必须是职责整理，不得复制 `Game.Content` 的正式内容装配规则形成第二套运行时。

---

## 10. 工作区设计

### 10.1 项目首页

显示：

- 当前 MOD 名称、ID、版本、作者和目录摘要。
- 最近编辑内容。
- 未解决问题摘要。
- 快速开始：新建角色、地图、剧情、物品、商店。
- 第一次制作 MOD 的步骤入口。
- 保存与备份说明。

首页不直接修改复杂数据，主要负责导航和项目状态。

### 10.2 角色与伙伴

布局：角色列表 + 角色详情 + 按需抽屉。

详情分区：

- 概览。
- 基础资料。
- 属性与成长。
- 武学与能力。
- 装备。
- 头像与资源。
- 引用关系。
- 高级数据。

头像必须同时展示：

- 当前字段值。
- 编辑器解析到的资源。
- 图片预览。
- 缺失原因。
- 使用统一资源选择器的入口。

### 10.3 地图与事件

布局：地图/点位树 + 地图画布 + 点位属性抽屉。

地图工具只出现在画布内部：

- 选择。
- 添加点位。
- 平移。
- 缩放。
- 重置视图。
- 网格或辅助显示（仅在现有数据表达允许时）。

点位详情：

- 名称和 ID。
- 图标和预览。
- 坐标。
- 已有显示/进入条件。
- 事件列表。
- 目标剧情、地图、商店、战斗等已有引用。
- 当前问题。

事件采用纵向卡片，明确区分条件、执行和目标，不改变现有事件模型。

### 10.4 剧情与任务

统一为一个工作区内的三种视图：

- 步骤视图（默认）。
- 图谱视图。
- DSL / JSON 高级视图。

默认步骤使用卡片表达：

- 对话。
- 选项。
- 命令。
- 条件。
- 跳转。

命令参数必须基于当前已支持的剧情命令，不在编辑器新增虚构命令。

### 10.5 物品与装备

按已有内容类型筛选和分区，避免一次展示全部字段：

- 基本信息。
- 图标和描述。
- 分类与使用。
- 价格。
- 需求。
- 效果。
- Affix。
- 引用关系。
- 高级数据。

资源和引用统一使用选择器，高级用户仍可查看原始 ID。

### 10.6 商店与经济

布局：商店列表 + 商品列表 + 商品详情。

商品摘要显示：

- 图标。
- 名称。
- 内容 ID。
- 价格或元宝价格。
- 限购。
- 问题状态。

添加商品使用内容选择器，不要求新人手写 ID。

### 10.7 资源管理

建立独立资源目录和统一选择器，支持当前后端已有的资源发现与预览能力：

- 类型过滤。
- 文件搜索。
- 缩略图或类型图标。
- 资源 ID。
- 注册状态。
- 引用次数。
- 缺失状态。
- 引用位置。

资源管理器不得改变运行时 PCK 覆盖原则，也不得把 loose assets 覆盖能力引入游戏运行时。

### 10.8 质量检查

把校验、头像检查、角色/物品检查、剧情图诊断等结果汇总到问题中心。

要求：

- 按错误、警告、建议过滤。
- 按内容类型和文件过滤。
- 点击跳转到工作区、对象和字段。
- 保留技术详情折叠区。
- 清楚说明问题来自后端正式校验还是编辑器辅助检查。

### 10.9 高级数据

承载尚未专用化的数据文件和高级用户能力：

- 文件树。
- JSON 编辑器。
- Story DSL。
- 格式化。
- 文件内搜索和跳转。
- 原始检查信息。

这保证专用工作区逐步迁移期间不会丢失现有编辑能力。

---

## 11. 状态与保存模型

重构必须先明确前端状态，不再依赖大量分散 DOM 状态推断。

建议状态至少分为：

```text
workspaceState
- 可用 MOD
- 当前 MOD
- 工作区路径摘要

navigationState
- 当前工作区
- 当前对象 ID
- 当前视图
- 抽屉状态

contentState
- 当前文件/对象
- 原始内容
- 编辑中内容
- 内容索引

editorState
- 新手/高级模式
- 搜索条件
- 列表筛选
- 展开分区
- 光标或当前步骤

dirtyState
- 是否修改
- 修改来源
- 最近保存结果

problemState
- 后端校验问题
- 编辑器即时问题
- 静态工具问题
- 当前筛选

preferenceState
- 主导航折叠
- 面板宽度
- 用户选择的默认视图
```

### 11.1 编辑器偏好存储边界

布局宽度、导航折叠等编辑器偏好不能写入 MOD JSON。初期可使用浏览器本地存储；若以后需要后端持久化，应保存在编辑器自己的配置位置，并单独设计，不与游戏存档或 MOD 设置混用。

### 11.2 保存原则

- 继续使用当前后端保存入口和备份机制。
- 保存前同步当前工作区编辑态到文件模型。
- 明确显示保存中、成功、失败和保存后的校验结果。
- 切换 MOD、文件或对象前处理未保存状态。
- 不因为切换视图而丢失尚未保存的数据。

---

## 12. 分阶段执行计划

## 阶段 0：重构基线与规范

### 目标

建立可执行的统一计划，冻结边界、术语、验证方式和阶段顺序。

### 交付物

- 本重构计划文档。
- 明确允许和禁止修改范围。
- 明确按钮术语和问题等级。
- 明确阶段完成汇报格式。
- 后续阶段的验收清单。

### 不做

- 不调整页面。
- 不移动前端代码。
- 不修改 API。
- 不修改 MOD 数据。

### 静态检查

- 文档路径存在。
- 阶段编号连续。
- 每阶段均包含目标、修改内容、静态检查和手动验证。
- 不可修改目录写清楚。
- 不包含要求 Codex 运行构建的步骤。

### 用户手动验证

本阶段只产生文档，不影响编辑器运行。用户只需阅读并确认：

1. 重构边界是否符合“不动游戏底层”。
2. 阶段顺序是否符合优先级。
3. 验证和汇报方式是否清楚。

---

## 阶段 1：设计系统与新页面壳层

### 目标

先建立稳定的视觉和布局骨架，不改变现有数据读写逻辑。

### 主要修改

1. 建立 `styles/tokens.css`、基础样式和标准组件样式。
2. 新建全局顶栏：MOD、全局状态、问题、保存。
3. 新建左侧主导航。
4. 新建中央工作区容器。
5. 新建右侧上下文抽屉容器，移除永久检查栏布局依赖。
6. 建立统一页面标题、按钮、徽标、空状态和状态栏。
7. 旧功能暂时通过“高级数据”工作区接入新壳层。
8. 建立宽屏、中屏、窄屏布局规则。

### 重点风险

- 现有 `app.js` 大量依赖固定 DOM ID。
- 顶栏按钮移动后事件绑定可能失效。
- Monaco 尺寸变化后需要重新 layout。
- 地图聚焦模式可能与新壳层冲突。

### 静态检查

- HTML 中所有脚本引用的 DOM ID 都存在。
- 被删除的 `.workspace-grid`、`.inspector` 等旧布局类没有遗留关键引用，或明确标记迁移适配。
- JavaScript 通过语法检查。
- CSS 设计令牌被组件引用，不继续增加散落颜色。
- 顶栏不再承载当前文件专用工具。
- Git diff 仅涉及 `tools/JsonEditor`。

### 用户手动验证

1. 启动编辑器并打开页面。
2. 确认当前 MOD 可以显示和切换。
3. 确认左侧导航可以展开、折叠和切换工作区。
4. 确认“高级数据”仍能打开现有文件。
5. 打开 JSON、表单、剧情和资产入口，确认旧能力仍可进入。
6. 修改一个安全测试字段但先不保存，确认未保存状态出现。
7. 保存，确认状态恢复。
8. 缩放浏览器或改变窗口宽度，检查导航和抽屉没有遮挡主要内容。
9. 反馈任何空白页、按钮无反应、布局溢出或 Monaco 尺寸错误。

---

## 阶段 2：前端核心模块与共享组件

### 目标

在不重写业务表单的前提下，把全局状态、API、导航、保存和通用 UI 从巨型 `app.js` 中抽离。

### 主要修改

1. 使用 `<script type="module">` 建立模块入口。
2. 抽离 `core/api.js`，集中所有 API 请求。
3. 抽离 `core/state.js`、`dirty-state.js`、`router.js`。
4. 抽离按钮、字段、对话框、抽屉、列表和问题列表组件。
5. 统一未保存离开、删除确认和错误展示。
6. 为旧工作区建立迁移适配层，避免一次重写全部表单。
7. 建立模块依赖规则，禁止工作区跨层直接操作其他工作区 DOM。

### 重点风险

- ES Module 加载路径和缓存版本。
- Monaco AMD loader 与 ES Module 共存。
- 全局函数被 HTML 内联行为或其他脚本引用。
- Story DSL 当前独立脚本的加载顺序。

### 静态检查

- 所有模块导入路径存在且大小写一致。
- 不存在循环依赖或未导出的引用。
- API 端点与 `Program.cs` 保持一致。
- 所有迁移后的 `fetch` 均集中或有明确例外。
- 普通脚本与模块脚本加载顺序清晰。
- JavaScript 静态语法检查通过。

### 用户手动验证

1. 页面无模块加载错误。
2. MOD 切换、文件加载、表单/JSON 切换正常。
3. 保存、格式化、检查正常。
4. Story DSL 打开、跳转、保存路径正常。
5. 刷新页面后能够恢复合理的导航入口。
6. 弹窗、抽屉和菜单可以用鼠标关闭，也可用 `Esc` 关闭。
7. 未保存切换时提示符合预期。

---

## 阶段 3：项目首页、高级数据与问题中心

### 目标

建立新人入口，同时把现有文件编辑能力完整收纳到“高级数据”，并统一散落检查结果。

### 主要修改

1. 项目首页：MOD 信息、快速开始、问题摘要、最近编辑占位或本地记录。
2. 高级数据工作区：文件树、表单/JSON/DSL、格式化、搜索和跳转。
3. 问题中心：汇总后端校验、头像检查、角色/物品检查和剧情诊断。
4. 问题跳转协议：工作区 + 对象 + 字段/行号。
5. 顶栏问题数量与问题中心同步。
6. 统一错误、警告、建议样式。

### 重点风险

- 不同检查工具返回结构不统一。
- 部分问题只有文件或文本，没有稳定对象定位。
- 最近编辑只属于编辑器偏好，不能写入 MOD。

### 静态检查

- 问题来源和等级映射有明确代码入口。
- 问题中心不改变后端正式校验结果含义。
- 快速开始只链接到已有或当前阶段已实现的能力。
- 高级数据完整保留现有文件入口。

### 用户手动验证

1. 进入首页，确认当前 MOD 信息正确。
2. 快速入口可以导航到对应工作区或明确显示尚未迁移。
3. 打开问题中心，执行检查。
4. 按严重程度和内容类型筛选。
5. 点击问题，确认可以定位到文件、对象、字段或代码行。
6. 确认头像检查等结果不再永久挤占右侧宽度。
7. 在高级数据中完成一次打开、修改、保存和重新读取。

---

## 阶段 4：角色与伙伴工作区

> 状态：已完成，用户已于 2026-07-11 手动验证通过。

### 目标

把现有角色专用表单迁移为统一、分区、可预览的新人工作区，并解决头像信息不透明的问题。

### 主要修改

1. 角色列表、搜索、状态筛选和问题徽标。
2. 角色概览和分区详情。
3. 基础字段统一组件化。
4. 头像字段显示原始值、解析结果、预览和错误原因。
5. 接入统一资源选择器。
6. 复制、删除和新建流程标准化。
7. 引用关系抽屉。
8. 保留角色原始 JSON 高级入口。

### 静态检查

- 所有字段继续写入当前角色 JSON 结构。
- 没有新增游戏不支持的角色字段。
- 头像解析使用统一资源索引，不复制另一套路径规则。
- 复制和新建使用安全默认结构。
- 删除前检查引用或明确提示无法完整判断。

### 用户手动验证

1. 搜索并打开“李文秀”。
2. 确认角色头像原始 ID、解析结果和图片预览一致。
3. 切换到其他角色，确认列表选择和表单内容同步。
4. 修改名称、描述等安全字段并保存，刷新后确认保留。
5. 使用资源选择器更换头像但先取消，确认数据未改变。
6. 复制角色，确认新 ID 不与原角色冲突。
7. 尝试删除被引用角色，确认危险提示明确。
8. 打开高级 JSON，确认与表单数据一致。

---

## 阶段 5：资源管理器与统一资源选择器

### 目标

在不降低阶段 4头像图库体验的前提下，把资源发现、解析、状态、选择和安全写入规则统一起来。

这里的“统一”指共享底层契约和交互规则，不要求头像、物品图标、地图图标、背景和音频使用同一种视觉布局。`resources.json` 同时包含图片、音频、文本模板和其他资源记录，资源管理器不得把全部记录错误地视为资产文件。

### 子阶段

#### 5A：资源契约与行为基线

- 盘点现有资源组、资产目录和四套资源选择流程。
- 集中图片、音频、文本和未知资源类型映射。
- 明确“可浏览”“资产支持”“通用接口允许注册”是三种不同能力。
- 抽取纯路径解析、扩展名判断和 value 规范化逻辑，并用行为测试锁定旧流程。
- 不改变阶段 4头像图库或其他现有选择器的视觉与写入行为。

#### 5B：只读资源管理器

- 分开显示已注册资源与原始资产文件。
- 展示资源 ID、组、value、真实路径、类型、重复、缺失、未注册和未引用状态。
- 引用位置只使用字段感知的静态索引，不把任意同值字符串误报成资源引用。
- 文本资源展示文本内容，不显示缺图错误。

#### 5C：共享选择器内核

- 统一搜索、类型筛选、键盘导航、选中状态、确认与取消语义。
- 头像继续使用大图图库和尺寸检查；物品、地图、背景和音频使用适合各自内容密度的视图。
- 取消不修改业务对象，确认后只写稳定资源 ID。

#### 5D：安全注册与修正

- 创建、复用和修正必须是三个明确动作。
- 所有直接写盘接口先完成全量预检，再备份，再写入。
- 同 ID异路径、资源组冲突和期望旧值不一致必须阻断。
- 修复物品图片上传绑定可能产生部分成功的问题。

#### 5E：消费端逐个迁移

- 按头像、物品、商店、地图顺序迁移到共享内核。
- 每迁移一个入口都单独检查视觉回归、中文输入、取消、确认、注册和修正。
- 未迁移入口继续保留原实现，不进行一次性替换。

### 主要修改

1. 资源管理工作区。
2. 类型过滤、搜索、缩略图、解析状态和注册状态。
3. 共享选择器内核与按领域定制的资源视图。
4. 统一图片预览失败状态。
5. 引用次数和引用位置（以当前可建立的静态索引为限）。
6. 整合头像检查和资源缺失问题。
7. 将角色、物品、商店和地图的资源选择逐步接到同一内核；剧情在阶段 9接入。

### 静态检查

- 不修改游戏运行时资源加载规则。
- 不读取工作区外资源。
- 不把编辑器预览路径写入正式 JSON。
- 上传/注册接口沿用当前编辑器后端能力。
- 资源类型映射集中管理。
- 阶段 4头像图库的预览密度、详情信息和尺寸检查不得退化。
- 文本资源不得因为没有资产文件而被报告为缺图。

### 用户手动验证

1. 搜索一个已有头像并预览。
2. 查看李文秀头像的资源 ID 和引用位置。
3. 搜索一个缺失资源，确认显示明确错误而不是空白。
4. 从角色或物品字段打开资源选择器，选择后确认字段更新。
5. 取消资源选择，确认原值不变。
6. 检查图片、音频等不同类型的预览状态。
7. 执行头像/资源检查并从问题中心定位。

---

## 原阶段 6：地图与事件工作区

> 执行编号调整为阶段 8。详细功能与验收清单保留在本节，实际顺序以第 13 节为准。

### 目标

建立真正以地图画布和点位事件为中心的工作区，解决交互点难以设置和多重分栏挤压问题。

### 主要修改

1. 地图列表和点位树。
2. 中央地图画布。
3. 画布内部工具栏。
4. 点位选择、拖动和坐标编辑同步。
5. 点位图标选择和预览。
6. 点位属性上下文抽屉。
7. 事件纵向卡片。
8. 条件、事件类型和目标引用使用已有定义的选择器。
9. 保存前即时问题提示。
10. 保留地图原始 JSON 高级入口。

### 静态检查

- 坐标换算只有一个集中入口。
- 画布拖动不会直接保存，仍经过编辑态和统一保存。
- 事件类型、条件字段和目标字段来自现有地图模型。
- 未识别事件字段被保留，不因表单编辑丢失。
- 地图画布工具不进入全局顶栏。

### 用户手动验证

1. 打开已有地图，确认地图背景和点位显示。
2. 选择不同点位，确认属性抽屉同步。
3. 拖动一个点位，确认坐标字段变化。
4. 不保存刷新，确认文件未被意外写入。
5. 再次拖动并保存，刷新后确认位置保留。
6. 添加点位并选择图标。
7. 添加地图、剧情、商店或战斗等已有事件类型。
8. 设置一个已有条件，检查自然语言摘要。
9. 删除事件，确认危险操作和撤销/确认逻辑。
10. 在游戏中进入对应地图，验证图标、位置和交互是否与编辑器一致。

---

## 原阶段 7：物品与装备工作区

> 执行编号调整为阶段 6。先用物品工作区验证资源选择、引用选择和嵌套条目编辑，再进入商店与地图。

### 目标

将现有物品专用表单迁移为按类型分区的标准工作区，并统一资源、需求、效果和 Affix 编辑。

### 主要修改

1. 物品列表、类型筛选和问题徽标。
2. 基本信息、资源、需求、效果、Affix 和高级数据分区。
3. 图标资源选择器。
4. 子项卡片和排序规范。
5. 新建物品类型向导。
6. 引用关系和商店使用位置。
7. 保留未知字段和高级 JSON。

### 静态检查

- 类型默认值只使用运行时已有结构。
- 未支持的物品效果不能被编辑器伪造。
- 需求、效果和 Affix 序列化结构保持不变。
- 子项删除不会错误删除相邻项。

### 用户手动验证

1. 分别打开普通物品、装备和武学书等不同类型。
2. 确认不同类型显示不同相关字段。
3. 更换图标并保存。
4. 添加、排序、复制和删除需求/效果/Affix 子项。
5. 切换高级 JSON，确认结构一致且未知字段未丢失。
6. 从问题中心定位无效内容引用。
7. 在游戏背包或商店中手动确认名称、图标和可用行为。

---

## 原阶段 8：商店与经济工作区

> 执行编号调整为阶段 7。商店依赖阶段 6提供稳定的物品内容投影与选择器。

### 目标

建立商店列表、商品列表和商品详情的标准三段式工作区，降低手输内容 ID 的需求。

### 主要修改

1. 商店搜索和列表。
2. 商品摘要表或卡片。
3. 内容选择器。
4. 银两价格、元宝价格和限购字段分组。
5. 商品复制、排序和删除。
6. 无效或被忽略商品的清晰提示。
7. 商店原始 JSON 高级入口。

### 静态检查

- `price`、`premiumPrice` 等现有字段语义保持不变。
- 当前被运行时忽略的旧商品规则只提示，不擅自改写。
- 内容选择器只列出可引用的已有内容类型。

### 用户手动验证

1. 打开一个已有商店。
2. 搜索并添加商品，不手写内容 ID。
3. 修改价格和限购并保存。
4. 调整商品顺序。
5. 检查重复商品和无效引用提示。
6. 在游戏中打开对应商店，确认商品、价格和限购显示。

---

## 阶段 9：剧情与任务工作区

### 目标

把剧情段、步骤、图谱和 DSL 统一到一个工作区，新人默认使用卡片式步骤，高级用户继续使用 DSL/JSON。

### 主要修改

1. 剧情文件/分组和 segment 树。
2. 对话、选项、命令、条件和跳转卡片。
3. 说话人选择器和头像预览。
4. 已支持剧情命令的参数表单。
5. 图谱作为工作区视图切换。
6. DSL/JSON 高级视图。
7. 无效跳转、缺失说话人和命令参数问题定位。
8. 新建 Story 流程移入剧情工作区。

### 静态检查

- 卡片序列化必须与当前 Story JSON/DSL 解析结构一致。
- 只提供当前应用层和宿主层已支持的命令。
- 未识别命令和字段必须可保留并进入高级模式。
- DSL 编译与保存仍使用现有后端入口。
- 图谱诊断不改变剧情执行规则。

### 用户手动验证

1. 打开现有剧情并在步骤、图谱、DSL/JSON 间切换。
2. 选择李文秀等说话人，确认名称和头像预览。
3. 添加一条对话、一个选项和一个已有命令。
4. 修改跳转目标，确认图谱同步。
5. 创建一个无效跳转，确认问题中心可以定位。
6. 保存 Story 源文件，确认编译预览正常。
7. 在游戏中触发剧情，确认对白、头像、选项和命令结果。

---

## 阶段 10：其他内容模块迁移

### 目标

按使用频率逐步专用化武学、Buff、成长、战斗、爬塔和世界规则；暂不适合专用化的内容继续使用高级数据。

### 候选顺序

1. 武学与能力：外功、内功、招式、绝技、天赋、奥义。
2. 战斗与挑战：战斗、Buff、爬塔。
3. 世界规则：世界触发器、门派、游戏配置、成长模板。
4. 其他低频数据。

### 准入条件

每种新专用表单开始前必须先确认：

- 当前运行时定义字段。
- 当前 JSON 示例。
- 引用关系。
- 未知字段保留策略。
- 新人任务是否足够明确。

不能为了“全部表单化”而制造低质量通用表单。

### 静态检查

- 每个专用表单的字段都能映射到现有 JSON 定义。
- 新建默认值来自已有样例或当前运行时允许的结构。
- 未识别字段和低频高级字段不会在表单保存时丢失。
- 新工作区使用共享字段、列表、选择器和问题组件。
- 未迁移数据类型仍可通过高级数据安全编辑。

### 用户手动验证

每迁移一种数据类型，单独提供对应字段编辑、保存、重新读取和游戏内验证清单。

---

## 阶段 11：帮助、新手引导与可用性收尾

### 目标

让新人不阅读源码也能完成第一条完整 MOD 内容链路。

### 主要修改

1. 首页“第一次制作 MOD”向导。
2. 任务式帮助：角色、地图点位、剧情、物品、商店。
3. 字段级帮助和示例。
4. 问题修复建议。
5. 保存后手动游戏验证提示。
6. 快捷键帮助。
7. 空状态和无结果状态统一。
8. 键盘焦点、ARIA 标签和基础无障碍检查。

### 静态检查

- 帮助入口和字段帮助不存在失效链接。
- 快速开始只引导到已经实现的工作区和操作。
- 交互控件具备可识别名称，图标按钮具备提示或 ARIA 标签。
- 空状态、错误状态和无结果状态使用统一组件。
- 教程不描述游戏运行时尚未支持的能力。

### 用户手动验证

邀请一名不了解项目文件结构的使用者，仅根据页面提示完成：

1. 找到角色入口。
2. 复制并重命名角色。
3. 选择头像。
4. 创建剧情对话。
5. 将剧情绑定地图点位。
6. 运行检查并保存。

记录卡住的位置和不理解的术语，再进行收尾调整。

---

## 阶段 12：遗留代码清理与后端职责拆分

### 目标

在所有核心工作区稳定后，删除迁移适配层、遗留 DOM、重复 CSS 和巨型文件中的旧实现。

### 主要修改

1. `app.js` 收缩为启动装配。
2. 删除已经迁移完成的旧表单函数。
3. 删除无引用 CSS。
4. 清理旧三栏布局和地图聚焦兼容分支。
5. 清理重复资源解析和按钮创建逻辑。
6. 按职责拆分 `Program.cs`。
7. 更新 `README.md` 和 `DEVELOPMENT.md`。

### 静态检查

- 搜索无引用 DOM ID 和 CSS class。
- 搜索重复 API 路径字符串。
- 搜索重复资源解析函数。
- 搜索遗留全局函数。
- 检查所有模块边界。
- 确认 Git diff 未涉及游戏底层目录。

### 用户手动验证

执行完整回归：

- MOD 切换。
- 首页。
- 所有已迁移工作区。
- 高级 JSON。
- Story DSL。
- 资源选择。
- 问题中心。
- 保存与备份。
- 响应式布局。
- 游戏内关键链路验证。

---

## 13. 阶段依赖与实际执行顺序

```text
阶段 0 基线
  ↓
阶段 1 壳层与设计系统
  ↓
阶段 2 核心模块与共享组件
  ↓
阶段 3 首页 / 高级数据 / 问题中心
  ↓
阶段 4 角色
  ↓
阶段 5A 资源契约与行为基线
  ↓
阶段 5B 只读资源管理器
  ↓
阶段 5C 共享选择器内核
  ↓
阶段 5D 安全注册与修正
  ↓
阶段 5E 逐个迁移资源入口
  ↓
阶段 6 物品与装备
  ↓
阶段 7 商店与经济
  ↓
阶段 8 地图与事件
  ↓
阶段 9 剧情
  ↓
阶段 10 其他模块
  ↓
阶段 11 新手引导
  ↓
阶段 12 清理与后端拆分
```

说明：

- 角色工作区已有完整头像图库；阶段 5必须以它为体验基线，不能替换成低信息密度的通用列表。
- 物品比地图更适合先验证资源选择器、引用选择器、嵌套条目和未知字段保留，因此调整到地图之前。
- 商店依赖稳定的物品内容投影和选择器，因此紧随物品工作区。
- 地图画布和事件编辑风险最高，在资源、物品和商店工作区稳定后迁移。
- 剧情最复杂，放在壳层、资源、问题和基础内容工作区稳定之后。
- 后端拆分最后进行，避免前后端同时大范围移动导致无法定位问题。
- 阶段 5允许只提取资源操作所需的纯服务和契约，不进行全局 `Program.cs` 拆分。

---

## 14. 每阶段完成定义（Definition of Done）

一个阶段只有同时满足以下条件才算完成：

1. 计划中的交付项已实现，未完成项已明确移入后续阶段。
2. 未修改游戏底层目录和 MOD 数据。
3. 已进行约定范围内的静态检查。
4. 已检查 Git diff，没有无关修改。
5. 已更新本计划中的阶段状态和完成记录。
6. 已向用户说明具体修改文件。
7. 已向用户逐条说明用户手动验证方法。
8. 已明确声明未运行构建、服务和游戏。
9. 若存在运行时不确定性，必须明确列出，不能以“应该可以”代替验证。
10. 用户完成手动验证并反馈通过后，才进入下一阶段的大范围修改。

---

## 15. 每阶段汇报模板

后续 Codex 完成每个阶段后，使用以下格式汇报：

```markdown
## 本阶段完成内容

- 修改了什么。
- 为什么这样调整。
- 哪些旧能力被保留。

## 修改文件

- `绝对路径`

## 静态检查结果

- 已检查的项目。
- 发现并修复的问题。
- 未运行的内容。

## 请你手动验证

1. 启动方式。
2. 页面操作步骤。
3. 每一步预期结果。
4. 重点观察的异常。

## 当前已知限制

- 尚未实现的内容。
- 需要后续阶段处理的内容。

## 下一阶段建议

- 下一步目标。
```

---

## 16. 手动验证反馈模板

用户发现问题时，建议按以下信息反馈：

```text
阶段：
当前 MOD：
浏览器与窗口大小：
所在工作区：
操作步骤：
预期结果：
实际结果：
是否保存过：
控制台错误（如有）：
截图：
```

这能帮助区分：

- 布局问题。
- 事件绑定问题。
- 数据加载问题。
- 保存问题。
- 资源解析问题。
- 游戏内运行结果与编辑器预览不一致。

---

## 17. 关键验收指标

### 17.1 新人可发现性

- 进入首页后 3 分钟内能找到角色、地图、剧情、物品和商店入口。
- 不知道 JSON 文件名也能完成主要制作任务。
- 不手写资源路径也能选择头像和图标。
- 不手写内容 ID也能选择常见引用。

### 17.2 布局

- 不再出现三层横向工具栏。
- 顶栏只包含全局操作。
- 右侧抽屉关闭时主工作区获得最大宽度。
- 地图画布在常见桌面宽度下可正常操作。
- 中等宽度可折叠主导航。

### 17.3 操作规范

- 每页只有一个明确主操作。
- 删除统一使用危险样式并与普通操作分组。
- “新建 / 添加 / 选择 / 检查 / 格式化 / 保存”术语统一。
- 低频操作进入更多菜单。
- 图标按钮有文本提示。

### 17.4 数据安全

- 不改变现有 JSON 语义。
- 不丢失未识别字段。
- 表单与高级 JSON 能正确同步。
- 未保存切换有明确处理。
- 保存前后状态清晰。
- 问题可以定位到对象和字段，或明确说明只能定位到文件。

### 17.5 可维护性

- 新功能不继续全部堆入 `app.js`。
- API 调用集中。
- 共享组件不被工作区重复实现。
- 资源解析入口统一。
- CSS 使用设计令牌和明确层级。
- 已迁移模块不再依赖旧三栏 DOM。

---

## 18. 当前阶段状态

| 阶段 | 名称 | 状态 |
|---|---|---|
| 0 | 重构基线与规范 | 已完成 |
| 1 | 设计系统与新页面壳层 | 已完成，用户已确认 |
| 2 | 前端核心模块与共享组件 | 已完成，用户已确认进入下一阶段 |
| 3 | 项目首页、高级数据与问题中心 | 已完成，用户已验证 |
| 4 | 角色与伙伴工作区 | 已完成，用户已验证 |
| 5 | 资源管理器与统一资源选择器（5A～5E） | 进行中：5A～5D 已完成，下一步 5E |
| 6 | 物品与装备工作区 | 未开始 |
| 7 | 商店与经济工作区 | 未开始 |
| 8 | 地图与事件工作区 | 未开始 |
| 9 | 剧情与任务工作区 | 未开始 |
| 10 | 其他内容模块迁移 | 未开始 |
| 11 | 帮助、新手引导与可用性收尾 | 未开始 |
| 12 | 遗留代码清理与后端职责拆分 | 未开始 |

---

## 19. 完成记录

### 2026-07-11：阶段 5D——安全注册与修正

已完成：

- 新增后端纯策略 `ResourceWritePolicy`，明确区分创建、复用和冲突，不再通过散落条件静默改写资源。
- 新增 JsonEditor 专项 .NET测试项目，覆盖资源缺失创建、完全一致复用、资源组冲突和 value冲突。
- 物品图片上传绑定改为“预检 → 用户确认 → 带状态令牌执行”的两步协议。
- 预检在读取上传数据和任何写盘前检查 `resources.json`、重复 ID、资源组、value、目标资产存在状态和修改时间。
- 同 ID异组、同 ID异 value和重复资源定义均阻断，不再静默把其他资源改成物品资源。
- 覆盖资产必须由前端明确确认；服务端未收到覆盖许可时返回冲突。
- 执行请求重新计算预检令牌；资源或资产在确认后发生变化时返回冲突，要求重新预检。
- 所有逻辑检查完成后才统一备份资产与 `resources.json`，然后执行写入。
- 前端确认摘要展示资源 ID、资源组、value、资产路径、创建/复用动作和创建/覆盖资产动作；取消不修改文件。

静态验证结果：

- `dotnet test tools/JsonEditor.Tests/JsonEditor.Tests.csproj`：4 项通过。
- `npm test`：13 项通过。
- `node --check`：入口通过。
- `dotnet build tools/JsonEditor/JsonEditor.csproj --no-restore`：通过，0 警告、0 错误。
- `git diff --check`：通过。

下一步：

- 阶段 5E按头像、物品、商店、地图顺序完成共享选择器适配和静态检查，不再修改资源写入契约。

### 2026-07-11：阶段 5C——共享选择器内核

已完成：

- 新增纯逻辑资源选择器模型，统一包含匹配、顺序模糊匹配、稳定排序、当前选中回退和方向导航。
- 搜索按单个字段独立评分，避免文件名、路径和资源 ID之间产生跨字段伪模糊匹配。
- 新增草稿选择会话，明确确认返回草稿值、取消恢复初始值的契约。
- 头像、物品、商店和地图四套现有资源弹层复用同一搜索与选中模型，原 DOM结构和领域操作按钮保持不变。
- 头像图库作为首个键盘适配器接入共享绑定：Esc关闭，方向键移动选择，原生按钮 Enter行为保留。
- 物品、商店和地图的键盘适配留到阶段 5E逐个完成，避免一次性替换交互。

静态验证结果：

- `npm test`：13 项通过。
- `node --check`：入口、资源选择模型和键盘绑定模块通过。
- `dotnet build tools/JsonEditor/JsonEditor.csproj --no-restore`：通过，0 警告、0 错误。
- `git diff --check`：通过。

下一步：

- 阶段 5D统一创建、复用和受控修正契约，先解决物品图片上传绑定的全量预检与部分成功风险。

### 2026-07-11：阶段 5B——只读资源管理器

已完成：

- 将原“资源浏览”入口升级为只读资源管理工作区，现有资源选择器和写盘入口保持不变。
- 提供“已注册资源”和“原始资产”两个视图，资源定义与共享资产文件不再混为一类。
- 资源列表展示 ID、资源组、value、类型、资产解析状态、重复 ID和字段感知静态引用状态。
- 文本资源显示文本 value，不因没有图片或音频文件而报告资产缺失。
- 原始资产视图显示图片、音频和其他文件的注册数量，并排除 `.import`、`.DS_Store` 等导入或隐藏元文件。
- 资源详情明确说明静态引用只覆盖已知资源字段，不宣称覆盖动态剧情参数。
- 列表单次最多渲染 400 条，中文搜索使用共享 IME安全输入绑定。

验证结果：

- `npm test`：8 项通过。
- `node --check`：入口、资源契约和资源工作区模块通过。
- `dotnet build tools/JsonEditor/JsonEditor.csproj --no-restore`：通过，0 警告、0 错误。
- 浏览器验证扩展 MOD 1,102 条资源可读取；`nick` 111 条文本资源无缺失资产误报。
- 浏览器验证原始资产视图、筛选、详情和 400 条渲染上限正常；控制台 0 错误。

下一步：

- 阶段 5C抽取共享选择器内核，但首先保持头像图库的双栏视觉、尺寸检查和注册流程不变。

### 2026-07-11：阶段 5A——资源契约与行为基线

已完成：

- 重新审计 `resources.json`，确认它同时包含图片、音频、文本模板和未知类型记录，不能整体视为资产表。
- 新增纯逻辑 `domain/resource-catalog.js`，集中资源组契约、资产类型判断、路径回退、艺术资源 value 规范化和重复 ID摘要。
- 明确通用注册接口只支持“场景、地图、音乐、音效、物品、头像”；战斗音乐、文本模板和其他旧资源组只做识别，不虚构注册能力。
- 现有 `app.js` 只委托路径查找、扩展名判断和 value 规范化；旧通用表单的文本 value 展示语义保持不变。
- 新增零构建 Node 行为测试，覆盖资源分类、注册能力、路径回退、文本资源、value 规范化和重复 ID。
- 浏览器回归确认阶段 4头像图库保持原双栏布局、314 个头像卡片、详情区和尺寸/注册流程，没有出现通用资源弹层。

验证结果：

- `npm test`：6 项通过。
- `node --check`：入口与资源契约模块通过。
- `dotnet build tools/JsonEditor/JsonEditor.csproj --no-restore`：通过，0 警告、0 错误。
- `git diff --check`：通过。
- 浏览器控制台：0 错误。

下一步：

- 阶段 5B只实现只读资源管理器，不接管现有头像、物品、商店或地图选择器。

### 2026-07-11：阶段 0——重构基线与规范

已完成：

- 建立模块化与标准化重构总计划。
- 固化不修改游戏底层和 MOD 数据的边界。
- 固化 Codex 只做静态检查、不运行构建和服务的约定。
- 定义目标信息架构、页面壳层、组件规范和模块目录。
- 定义 0～12 阶段的目标、风险、静态检查和手动验证步骤。
- 定义阶段完成标准、汇报模板和用户反馈模板。

未执行：

- 未修改编辑器页面和功能。
- 未修改后端 API。
- 未运行构建、服务、测试或 Godot。

下一步：

- 用户确认本计划后，进入阶段 1：设计系统与新页面壳层。

### 2026-07-11：阶段 1——设计系统与新页面壳层

已完成：

- 重建编辑器 HTML 页面壳层，保留全部现有持久 DOM ID 与旧功能挂载点。
- 将顶部三层混合操作重新分层：顶栏只保留 MOD、检查、问题上下文、未保存状态、保存和帮助菜单。
- 将“格式化”移动到当前内容工具栏，不再作为全局命令。
- 将工作区绝对路径从顶栏移动到左侧导航底部摘要。
- 将固定右侧检查栏改为按需打开的“问题与上下文”抽屉。
- 增加抽屉遮罩、关闭按钮、`Esc` 关闭和焦点返回行为。
- 建立左侧主导航，将现有能力明确为“高级数据 / 剧情图谱 / 资源浏览”。
- 增加导航折叠功能，并使用编辑器本地存储记住折叠状态。
- 增加工作区眉题和列表标题，使当前模式具有稳定的空间表达。
- 根据当前模式显示相关快捷操作，减少无关按钮常驻。
- 建立 `tokens.css`，定义颜色、间距、圆角、阴影、控件高度和布局尺寸。
- 建立 `components.css`，定义主按钮、次按钮、轻量按钮、危险按钮、图标按钮、状态徽标和分段控件。
- 建立 `shell.css`，承载顶栏、导航、主工作区、上下文抽屉和三档响应式布局。
- 为现有旧表单保留设计令牌别名，避免阶段 1 同时重写全部业务表单。
- 保留现有表单、JSON、Story DSL、剧情图、资源浏览、地图工作台、保存和检查逻辑。

修改文件：

```text
tools/JsonEditor/wwwroot/index.html
tools/JsonEditor/wwwroot/app.js
tools/JsonEditor/wwwroot/styles/tokens.css
tools/JsonEditor/wwwroot/styles/components.css
tools/JsonEditor/wwwroot/styles/shell.css
tools/JsonEditor/REFACTORING_PLAN.md
```

静态检查：

- `app.js` 与 `story-dsl.js` 已通过 `node --check`。
- HTML 共 49 个持久 ID，无重复 ID。
- `app.js` 中静态 `getElementById` 持久引用均能在 HTML 中找到。
- 原有未使用的 `speakerToolBox` 空引用已删除。
- 旧样式与三个新增样式文件的大括号结构检查通过。
- `git diff --check` 通过。
- 修改范围仅位于 `tools/JsonEditor`。
- 未运行 `.NET build`、服务、测试、Godot 或浏览器运行时测试。

等待用户手动验证：

- 页面首次加载与 MOD 切换。
- 左侧导航折叠和刷新后的状态记忆。
- 高级数据、剧情图谱和资源浏览切换。
- 表单、JSON、Story DSL、地图工作台和 Monaco 尺寸。
- 上下文抽屉的打开、遮罩关闭、按钮关闭与 `Esc` 关闭。
- 检查状态、保存状态和未保存状态。
- 宽屏、中屏和窄屏布局。

下一步：

- 用户确认阶段 1 手动验证通过后，进入阶段 2：前端核心模块与共享组件。

### 2026-07-11：阶段 2——前端核心模块与共享组件

已完成：

- 将 `app.js` 切换为浏览器原生 ES Module 入口，保留 Monaco AMD loader 与 Story DSL 经典脚本在模块入口之前加载。
- 抽离 `core/state.js`，集中创建和导出编辑器全局状态，同时保留原有 `Map`、`Set` 和状态字段语义。
- 抽离 `core/api.js`，集中 JSON 请求、活动 MOD 参数追加和统一 API 错误对象；`app.js` 不再直接调用 `fetch`。
- 抽离 `core/preferences.js`，集中导航折叠、活动 MOD 和最近文件的本地偏好读写。
- 抽离 `core/dirty-state.js`，集中未保存状态、离开确认和状态变化事件；原有业务写入通过迁移适配调用。
- 建立 `core/events.js` 的零依赖事件总线，作为后续工作区解耦的基础。
- 建立 `core/commands.js`，集中注册保存与格式化快捷键，移除全局键盘处理中的重复快捷键判断。
- 建立 `core/router.js` 的工作区模式与路由值规范，当前先用于模式归一化，不提前实现复杂 URL 路由。
- 抽离 `ui/shell.js` 与 `ui/drawers.js`，集中导航折叠、上下文抽屉、模式标题和检查状态。
- 抽离 `ui/buttons.js`、`ui/fields.js`、`ui/dialogs.js`、`ui/lists.js` 与 `ui/problem-list.js`。
- 将旧的按钮、字段、工具弹窗、地图问题列表和 Story DSL 校验显示改为共享组件的兼容包装。
- 将点位、事件、商品、普通记录删除确认统一经过 `confirmAction`。
- 工具弹窗增加统一的遮罩关闭和 `Esc` 关闭入口。
- 保留角色、地图、物品、商店、剧情、资源等现有业务编辑器逻辑，没有在本阶段重写数据表单。
- 未修改 `Program.cs`、游戏底层目录或 MOD 数据。

新增文件：

```text
tools/JsonEditor/wwwroot/core/api.js
tools/JsonEditor/wwwroot/core/commands.js
tools/JsonEditor/wwwroot/core/dirty-state.js
tools/JsonEditor/wwwroot/core/events.js
tools/JsonEditor/wwwroot/core/preferences.js
tools/JsonEditor/wwwroot/core/router.js
tools/JsonEditor/wwwroot/core/state.js
tools/JsonEditor/wwwroot/ui/buttons.js
tools/JsonEditor/wwwroot/ui/dialogs.js
tools/JsonEditor/wwwroot/ui/drawers.js
tools/JsonEditor/wwwroot/ui/fields.js
tools/JsonEditor/wwwroot/ui/lists.js
tools/JsonEditor/wwwroot/ui/problem-list.js
tools/JsonEditor/wwwroot/ui/shell.js
```

修改文件：

```text
tools/JsonEditor/wwwroot/app.js
tools/JsonEditor/wwwroot/index.html
tools/JsonEditor/REFACTORING_PLAN.md
```

迁移边界：

- `app.js` 仍保留现有业务工作区和表单实现，本阶段只抽离核心基础设施与共享组件。
- `story-dsl.js` 暂时继续通过 `window.StoryDsl` 提供能力，避免同时重写 DSL 解析器。
- Monaco 暂时继续使用项目内置 AMD loader，模块入口只负责消费已加载的 `window.require`。
- 删除确认当前统一经过共享适配器，但仍使用浏览器原生确认框；后续可在不影响调用方的前提下替换为标准模态框。
- 事件总线与路由模块先建立稳定接口，后续工作区迁移时再逐步替换直接 DOM 协作。

静态检查：

- `app.js`、`story-dsl.js`、全部 `core/*.js` 与 `ui/*.js` 已通过 `node --check`。
- ES Module 相对导入路径、命名导出和循环依赖检查通过。
- HTML 持久 ID 无重复；`app.js` 的静态 DOM 引用均能对应持久节点或明确的运行期弹层节点。
- 前端使用的 16 个 API 路径均能在 `Program.cs` 找到对应端点。
- 编辑器自有代码的网络请求集中在 `core/api.js`；未在业务工作区保留直接 `fetch`。
- `app.js` 中旧的 `state`、`storageKeys`、`requestJson` 和 `withActiveMod` 本地定义已移除。
- HTML 不存在内联事件处理器，模块化后不依赖顶层函数自动挂载到 `window`。
- `git diff --check` 通过，修改范围仅位于 `tools/JsonEditor`。
- 未运行 `.NET build`、服务、测试、Godot 或浏览器运行时测试。

等待用户手动验证：

- 页面模块加载、Monaco 和 Story DSL 全局桥接。
- MOD 切换、导航偏好和最近文件恢复。
- 高级数据、剧情图谱和资源浏览切换。
- 表单、JSON、DSL、保存、格式化和快捷键。
- 未保存切换确认、删除确认、工具弹窗与上下文抽屉。
- 地图工作台、资源选择弹层和窄屏布局。

下一步：

- 用户确认阶段 2 手动验证通过后，进入阶段 3：项目首页、高级数据与问题中心。

### 2026-07-11：阶段 2 手动验证修正——版本与核心加载指示

原因：

- 用户手动验证时页面可以显示，但按钮没有响应，需要区分旧 HTML 缓存与 ES Module 加载失败。

已完成：

- 顶栏增加静态编辑器版本 `v0.2.1 · 阶段 2`，该文本不依赖 JavaScript。
- 增加核心状态指示，HTML 默认显示红色“核心未加载”。
- `app.js` 成功执行后改为绿色“核心已加载”。
- 浏览器标签标题增加 `v0.2.1`。
- 新增 `core/version.js` 作为前端版本单一来源。
- 更新 `app.js` 与 `shell.css` 缓存版本参数。

判断方式：

- 看不到 `v0.2.1`：仍在使用旧 HTML 或旧服务内容。
- 看到 `v0.2.1`，但显示“核心未加载”：ES Module 导入或执行失败，需要查看浏览器控制台第一条红色错误。
- 看到绿色“核心已加载”：模块入口正常执行，应继续检查具体按钮事件。

静态检查：

- 16 个模块文件语法和导入路径检查通过。
- HTML 51 个持久 ID 无重复，版本节点引用存在。
- CSS 结构与 `git diff --check` 通过。
- 未运行构建、服务或浏览器。

### 2026-07-11：阶段 2 手动验证修正——模块启动诊断

确认现象：

- 用户截图显示 `v0.2.1`，证明新 HTML 已加载。
- 状态仍为“核心未加载”，因此故障位于 ES Module 导入或模块执行阶段，而不是具体按钮事件。

已完成：

- 版本提升为 `v0.2.2`。
- 新增经典脚本 `bootstrap.js`，由它动态导入 ES Module 入口。
- 动态导入失败时，将错误信息直接显示在页面顶部，并同时写入浏览器控制台。
- 将所有 `app.js -> core/ui` 导入增加统一缓存版本 `20260711-core-4`。
- 将 `ui/shell.js -> ui/drawers.js` 导入增加相同缓存版本。
- 更新 `app.js`、`bootstrap.js` 和 `shell.css` 页面缓存参数。

手动判断：

- `v0.2.2 + 核心已加载`：模块入口和全部静态依赖已执行。
- `v0.2.2 + 核心加载失败`：页面顶部会显示具体错误，应根据该错误继续修复。
- 仍显示 `v0.2.1`：浏览器或服务仍在提供旧 HTML。

静态检查：

- `bootstrap.js`、`app.js`、Story DSL 和全部模块通过 `node --check`。
- 带查询参数的模块路径检查通过。
- HTML 53 个持久 ID 无重复，诊断节点完整。
- CSS 结构与 `git diff --check` 通过。
- 未运行构建、服务或浏览器。


### 2026-07-11：阶段 2 手动验证修正——ES Module 重复声明

确认现象：

- 用户截图中的页面诊断为 `Identifier 'hasDefinitionOfType' has already been declared`。
- 这说明动态启动器已经正确捕获故障，根因是旧单文件脚本中存在两个同名顶层函数；经典脚本迁移到 ES Module 后，重复声明会阻止整个模块解析，因而所有按钮都无法注册事件。

已完成：

- 将两处 `hasDefinitionOfType` 收敛为一个共享实现，不保留兼容分支。
- 统一规范 definition id：仅接受字符串、去除首尾空白、空值直接返回不存在。
- 版本提升为 `v0.2.3`。
- 模块缓存版本提升为 `20260711-core-5`，启动器缓存版本提升为 `20260711-bootstrap-2`，避免浏览器继续复用解析失败的旧入口。

手动判断：

- 顶栏应显示 `v0.2.3 · 阶段 2`。
- 正常情况应显示绿色“核心已加载”，随后再验证导航、工具按钮和编辑器操作。
- 如果仍显示“核心加载失败”，以页面红色错误条的新错误为准继续修正。

静态检查：

- 运行 ES Module 解析级语法检查，确认不再存在该重复声明。
- 检查模块导入路径、HTML 持久 ID、缓存版本和 `git diff --check`。
- 未运行构建、服务、测试、Godot 或浏览器自动化。


### 2026-07-11：阶段 3——项目首页、高级数据与问题中心

阶段状态：

- 代码重构已完成。
- 静态检查已通过。
- 等待用户按本节步骤进行浏览器手动验证。

已完成架构：

- 将 `home` 与 `problems` 纳入正式工作区路由，与现有 `data`、`story`、`assets` 并列。
- 编辑器启动与切换 MOD 后默认进入项目首页，不再把新人直接送入原始 JSON 文件。
- 现有文件树、表单、JSON、Story DSL、格式化、搜索、跳转和保存能力完整保留在“高级数据”。
- 新增统一前端问题模型，严重程度固定为 `error`、`warning`、`suggestion`，并统一携带来源、内容类型和可选定位信息。
- 新增问题定位协议：`workspace + path + line/column + definitionId/definitionTypes`；无法稳定定位的问题明确显示为“仅供参考”。
- 最近编辑记录按 MOD ID 隔离，仅写入浏览器偏好存储，不写入 MOD 数据。

项目首页：

- 展示当前 MOD 的名称、ID、版本、作者、日期、描述和数据目录状态。
- 展示数据文件、内容定义、剧情段和共享资源数量。
- 展示统一问题摘要，并可直接进入问题中心。
- 提供角色、地图、剧情、物品、商店、资源、高级数据和问题中心快速入口。
- 阶段 4 之后才会专用化的工作区不会伪装成已完成模块；当前明确回落到已有高级数据文件。
- 展示最近编辑的数据文件和资源入口。

问题中心：

- 汇总正式后端内容校验、编辑器内容索引、重复定义、剧情图谱、头像静态检查、当前 Story DSL 即时检查，以及当前角色/物品表单辅助检查。
- 后端 `/api/validate` 仍按原有 `ok + message` 含义展示，不伪造后端未提供的结构化诊断。
- 支持全部、错误、警告、建议数量切换。
- 支持问题文本、内容类型和问题来源筛选。
- 支持展开技术详情。
- 顶栏问题数量与首页、问题中心使用同一份问题集合。
- 数据、剧情和已存在资源问题可以定位；缺少稳定路径的问题不会提供虚假的定位按钮。

高级数据与导航修正：

- 从首页或资源浏览进入高级数据时，只有当前路径确实属于数据文件才会复用，否则恢复该 MOD 最近数据文件或打开首个数据文件。
- 切换 MOD 时同步刷新数据文件、共享资源文件、内容索引、剧情图谱、最近编辑和正式校验，再回到项目首页。
- 最近资源记录只有在资源仍存在时才允许打开。
- 问题定位前检查数据路径或资源路径是否仍存在，避免对空路径和失效最近记录发起请求。

编辑器后端信息补充：

- `Program.cs` 的编辑器工作区摘要增加读取现有 `mod.json` 的 `author` 与 `date`，供项目首页展示。
- 未修改 `mod.json`，未新增游戏字段，未改变 MOD 或游戏运行时数据语义。

新增文件：

- `wwwroot/core/problems.js`
- `wwwroot/core/recent-items.js`
- `wwwroot/ui/home.js`
- `wwwroot/ui/problem-center.js`
- `wwwroot/styles/stage3.css`

本阶段主要修改文件：

- `Program.cs`
- `wwwroot/app.js`
- `wwwroot/index.html`
- `wwwroot/bootstrap.js`
- `wwwroot/core/preferences.js`
- `wwwroot/core/router.js`
- `wwwroot/core/state.js`
- `wwwroot/core/version.js`
- `wwwroot/ui/shell.js`
- `REFACTORING_PLAN.md`

版本与缓存标识：

- 编辑器版本：`v0.3.0 · 阶段 3`。
- ES Module 缓存版本：`20260711-core-8`。
- 启动器缓存版本：`20260711-bootstrap-5`。

静态检查结果：

- 22 个编辑器 JavaScript 文件通过 ES Module 解析级语法检查。
- 21 个相对模块导入全部能解析到实际文件。
- HTML 共 64 个持久 ID，无重复 ID。
- `app.js` 共 64 个持久 DOM 引用全部能对应 HTML 节点；4 个运行期弹层 ID 单独排除并确认属于动态节点。
- `styles.css` 与 `styles/*.css` 结构括号检查通过。
- `ModSummary` 静态检查确认 8 个字段、2 个构造位置，字段顺序一致。
- 版本号与缓存参数一致，当前运行文件中无旧的 `core-5` / `core-6` 引用。
- `git diff --check` 通过。
- 修改范围保持在 `tools/JsonEditor`。

未执行：

- 未运行 `.NET build`、`.NET run`、`.NET test`。
- 未启动编辑器服务。
- 未运行 Godot。
- 未运行浏览器或浏览器自动化。

阶段 3 手动验证：

1. 重启现有编辑器服务，并对页面执行强制刷新。
2. 顶栏确认显示 `v0.3.0 · 阶段 3` 和绿色“核心已加载”。
3. 项目首页核对 MOD 名称、ID、版本、作者、日期、描述与数据目录状态。
4. 核对数据文件、内容定义、剧情段、共享资源数量是否合理。
5. 依次点击角色、地图、剧情、物品、商店、资源、高级数据、问题中心快速入口；尚未专用化的模块应进入对应高级数据文件。
6. 在高级数据打开文件，切换表单/JSON/DSL，测试搜索、格式化、修改、保存和重新打开。
7. 回到首页，确认最近编辑出现；打开资源后确认资源也进入最近编辑。
8. 进入问题中心点击“重新检查”，测试严重程度、内容类型、问题来源和文本搜索；连续输入多个字符时搜索框不能丢失焦点。
9. 展开问题技术详情，并分别测试可定位的数据、剧情或资源问题；无稳定位置的问题应显示“仅供参考”。
10. 核对顶栏问题数量与问题中心“全部”数量一致。
11. 切换 MOD，确认返回项目首页、MOD 信息更新、最近编辑按 MOD 隔离。
12. 打开“当前上下文”抽屉，确认它仍与问题中心分离且可正常关闭。
13. 缩窄浏览器窗口，检查首页卡片、问题筛选、问题行和顶栏按钮没有明显溢出。


### 2026-07-11：阶段 3 手动验证修正——MOD 作者与日期未显示

确认现象：

- `mod.json` 已包含 `author` 与 `date`。
- 项目首页能显示名称、版本和描述，但作者与日期显示“未填写”。
- 这说明新版静态前端已经加载，而当前运行中的编辑器后端仍是旧程序集；旧 `/api/workspace` 响应没有 `author` 与 `date` 字段。

已完成：

- `Program.cs` 的 `ModSummary` 已读取并返回现有 manifest 的 `author` 与 `date`。
- 项目首页现在区分“字段存在但为空”和“旧后端根本没有返回字段”。
- 旧后端场景不再误报“未填写”，而是显示“服务未刷新”，并提示重启编辑器服务。
- ES Module 缓存版本提升为 `20260711-core-8`，启动器缓存版本提升为 `20260711-bootstrap-5`。

手动判断：

- 显示“服务未刷新”：页面仍连接旧版编辑器后端，需要停止并重新启动编辑器服务。
- 重启后显示 `zheng` 与 `2026-07-08`：新后端已经生效。
- 重启后仍显示“未填写”：新版后端已经生效，但对应 manifest 字段确实为空。

静态检查：

- 未运行构建、服务或浏览器。
- JavaScript 模块语法、导入路径、HTML ID、CSS 结构、C# `ModSummary` 字段与构造参数、缓存标识及 `git diff --check` 均通过。

### 2026-07-11：阶段 3 用户手动验证通过

- 用户已完成阶段 3 手动验证并确认通过。
- 阶段 3 正式结束；下一阶段为阶段 4“角色与伙伴工作区”。
- 本次状态确认不开始阶段 4 的实现。

### 2026-07-11：阶段 4——角色与伙伴工作区实现完成

已完成：

- 新增独立“角色与伙伴”导航与工作区，不再把角色入口伪装成高级数据文件。
- 角色列表支持搜索、对白/入队/战斗/头像缺失/配置问题筛选、当前选择和问题数量徽标。
- 角色详情按概览、属性、武学与成长、装备与天赋、高级 JSON 分区。
- 基础字段和属性仅写入现有 `CharacterDefinition` 支持的字段，没有增加编辑器专用游戏字段。
- 头像区域显示原始资源 ID、资源索引解析结果、图片预览和明确错误原因，并复用现有头像资源选择器及资源解析入口。
- 新建角色使用安全最小结构；复制自动生成唯一 ID；删除前展示按角色 ID/名称得到的静态精确字符串引用，并明确提示动态引用无法完整判断。
- 新增引用关系抽屉；内容索引增加 `referencesByValue`，记录文件路径、JSON 字段路径和所属定义。
- 高级 JSON 直接替换当前内存中的同一条角色记录，解析失败不修改数据，未识别字段会保留。
- 顶栏保存继续写入现有 `characters.json`；表单、高级 JSON 和隐藏文本编辑器共用 `state.formRecords` / 同一保存源。
- 角色专项问题可以从问题中心定位回独立角色工作区和具体角色。
- 离开有未保存修改的工作区时先确认；确认丢弃后重新读取当前数据文件。

新增文件：

- `wwwroot/core/input-composition.js`
- `wwwroot/ui/characters.js`
- `wwwroot/ui/reference-picker.js`
- `wwwroot/styles/stage4.css`

本阶段主要修改文件：

- `Program.cs`
- `wwwroot/app.js`
- `wwwroot/index.html`
- `wwwroot/bootstrap.js`
- `wwwroot/core/router.js`
- `wwwroot/core/state.js`
- `wwwroot/core/version.js`
- `wwwroot/ui/shell.js`
- `REFACTORING_PLAN.md`

静态检查结果：

- 25 个编辑器自有 JavaScript 文件通过 `node --check`。
- 27 个相对 ES Module 导入全部解析到实际文件。
- HTML 共 66 个持久 ID，无重复 ID。
- `app.js` 的 62 个持久 DOM 引用全部对应 HTML 节点；4 个运行期资源选择器弹层 ID 单独确认属于动态节点。
- `styles.css` 与 `styles/*.css` 结构括号检查通过，阶段 4 样式复用现有设计令牌和按钮类型。
- 新建角色模板字段与当前 `CharacterDefinition` 字段集合一致，没有编辑器新增游戏字段。
- 当前修改范围仅位于 `tools/JsonEditor`。
- `git diff --check` 通过。

版本与缓存标识：

- 编辑器版本：`v0.4.0 · 阶段 4`。
- ES Module 缓存版本：`20260711-core-17`。
- 启动器缓存版本：`20260711-bootstrap-14`。
- 阶段 4 样式缓存版本：`20260711-stage4-8`。

未执行：

- 未运行 `.NET build`、`.NET run`、`.NET test`。
- 未启动编辑器服务。
- 未运行 Godot。
- 未运行浏览器或浏览器自动化。

阶段状态：

- 阶段 4 已完成并由用户手动验证通过。
- 下一阶段为阶段 5“资源管理器与统一资源选择器”。

### 2026-07-11：阶段 4 手动验证修正——中文输入与角色引用编辑

确认问题：

- 角色搜索框在每次 `input` 时重绘整个工作区，破坏中文输入法的 composition 过程，导致中文无法正常写入。
- 模型与成长模板仍是手写文本；外功、内功仍暴露数组 JSON；绝技、天赋和装备仍要求手写 ID，不符合阶段 4 面向新人的目标。

已修正：

- 搜索输入增加 `compositionstart` / `compositionend` 处理，只在中文输入法完成组词后刷新角色列表。
- 模型下拉从当前内容中已使用的 `model` 值建立选项；成长模板从内容定义索引建立选项。
- 外功、内功改成结构化行编辑：按名称选择定义，并直接编辑等级、上限和内功装备状态。
- 绝技、天赋、初始装备改成名称化下拉选择、已选列表和删除操作。
- 所有引用控件展示“名称（ID）”，正式 JSON 仍保存稳定 ID，不把显示名称误写成运行时引用。
- 保留不存在的旧引用并标注“当前引用不存在”，避免打开表单时静默丢失未知数据。
- 新增共享 `ui/reference-picker.js`，支持中文名称、稳定 ID、说明和关键属性的包含匹配与顺序模糊匹配。
- 搜索结果支持键盘上下选择、回车添加、Esc 关闭，每次最多展示最相关的 40 条结果。
- 外功、内功、绝技、天赋和装备的搜索结果与已选项展示图标、说明和类型相关属性摘要。
- 技能图标从 `assets/art/icon` 解析，装备图片继续通过现有资源索引解析；无法解析时显示类型占位和明确状态。
- 角色引用选项使用编辑器投影 `{ id, name, type, iconId, iconPath, subtitle, description, metadata, searchText }`，不向正式 MOD JSON写入展示字段。
- 基础包静态核对确认：158/158 外功、43/43 内功、27/27 绝技图标可从 `assets/art/icon` 解析，104/104 装备图片资源已注册。

缓存标识提升：

- ES Module：`20260711-core-17`。
- Bootstrap：`20260711-bootstrap-14`。
- 阶段 4 样式：`20260711-stage4-8`。

### 2026-07-11：阶段 4 手动验证修正——保存后旧角色表单重新出现

确认问题：

- 独立角色工作区保存成功后，通用 `refreshFormFromEditor()` 仍按 `viewMode = form` 调用阶段 4之前的角色表单渲染。
- 该流程会移除旧 `formView` 的隐藏状态，导致新角色工作区与旧角色表单同时存在，页面看起来回到了重构前界面。

已修正：

- `renderFormView()` 在 `characters` 模式下立即隔离旧表单，不再创建或显示旧角色表单 DOM。
- 保存、丢弃重读和表单数据同步后，角色模式始终保持新角色工作区，头像选择器仍可正常刷新。
- ES Module 缓存提升为 `20260711-core-17`，Bootstrap 缓存提升为 `20260711-bootstrap-14`。

### 2026-07-11：阶段 4 手动验证修正——统一搜索输入与对白角色注册入口

确认问题：

- 头像、物品图片、商店资源和地图资源等旧选择器仍在 `input` 事件中立即重绘弹层，中文输入法组词会被打断。
- 阶段 4独立角色工作区没有暴露原有“新建说话人”工具，用户只能看到“新建完整角色”，两种不同用途被界面混淆。

已修正：

- 新增 `core/input-composition.js`，集中提供 IME 安全的搜索输入绑定。
- 角色、剧情、问题中心、高级数据记录、地图点位、头像、物品图片、商店资源、地图资源和共享引用选择器统一使用同一输入绑定。
- 中文输入法只有在 composition 完成后才触发筛选和重绘；普通键盘输入仍实时筛选。
- 角色列表新增独立“对白角色”入口，复用现有 `/api/static/speaker` 正式写入流程。
- 对白角色功能明确为“最小角色定义 + 可选头像资源注册”，与创建带完整属性、成长、武学和装备结构的伙伴角色保持区分。
- 对白角色注册成功后重新读取 `characters.json`，保持在新角色工作区并自动选中新角色。
- ES Module 缓存提升为 `20260711-core-17`，Bootstrap 缓存提升为 `20260711-bootstrap-14`，阶段 4样式缓存提升为 `20260711-stage4-8`。

### 2026-07-11：阶段 4 手动验证修正——IME 去重与头像尺寸策略调整

确认问题：

- 共享 IME监听通过“跳过下一次 input”去重 composition 尾随事件；部分浏览器没有该尾随事件，导致下一次正常输入被错误吞掉。
- 512×512 被实现成注册和使用硬门槛，限制了希望保留高分辨率原图的 MOD作者，也把运行时缩放职责错误推给素材。

已修正：

- IME监听改为按最后已发送的输入值去重，不再跳过未来事件；中文 composition 完成后发送一次，同值尾随事件自然忽略。
- 头像选择器继续展示真实尺寸；512×512 调整为推荐规范，不再阻止大图或其他可读取尺寸图片注册和使用。
- PNG仍保留可选“备份并规范化为 512×512”，但是否执行由用户决定。
- 后端头像资源入口只强制图片真实存在且尺寸元数据可读取，不再限制固定像素。
- 编辑器不处理游戏内头像框的缩放、裁切或越界表现；大图进入游戏后的显示方式属于运行时表现层责任，不在本重构范围内修改。
- ES Module 缓存提升为 `20260711-core-17`，Bootstrap 缓存提升为 `20260711-bootstrap-14`，阶段 4样式缓存提升为 `20260711-stage4-8`。

### 2026-07-11：范围边界修正——禁止修改游戏运行时

- 用户再次明确：本重构只能完善编辑器，不允许修改游戏底层代码、Godot场景或运行时表现。
- 已撤回 `scenes/ui/avatar_box/avatar_box.tscn` 的头像缩放配置，工作区不保留该越界修改。
- 编辑器对非 512×512 头像只提供尺寸信息、风险提示和可选规范化，不阻止使用，也不承诺修复游戏内显示。
- 后续阶段继续严格限制在 `tools/JsonEditor`，除非用户另行明确改变范围。

### 2026-07-11：阶段 4 手动验证修正——同 ID错误头像路径的受控修复

确认问题：

- `头像.清兵` 已存在，但旧 value 为 `head/清兵`；实际已规范化图片是 `art/head/qingbing.png`，正确 value 应为 `head/qingbing`。
- 头像选择器只能报告“资源 ID已存在”，无法区分同图复用、错误路径修正和非头像资源冲突。

已修正：

- 头像资源输入现在区分四种状态：新建、同图复用、头像路径修正、非头像资源阻断。
- 同 ID且 value 指向当前图片时直接显示“使用已有资源”。
- 同 ID头像资源指向其他或不存在路径时，显示当前 value 与目标 value，并提供“修正资源路径并使用”。
- 新增 `PUT /api/static/portrait-resource`：保留稳定资源 ID和资源组，只更新 value；更新前校验目标图片存在且尺寸元数据可读取。
- 更新请求携带期望旧 value，若资源在打开选择器后已被其他操作修改则返回冲突，不覆盖新状态。
- 修改 `resources.json` 前自动备份；修正后重建索引、写回当前角色 portrait，并立即刷新头像选择器。
- `头像.清兵` 的预期修复结果是保持 ID不变，仅把 value 从 `head/清兵` 改为 `head/qingbing`。
- ES Module 缓存提升为 `20260711-core-17`，Bootstrap 缓存提升为 `20260711-bootstrap-14`，阶段 4样式缓存提升为 `20260711-stage4-8`。

### 2026-07-11：阶段 4 手动验证修正——头像中文 ID与尺寸检查

确认问题：

- 头像选择器过去按图片文件名建议资源 ID，因此 `qingbing.png` 会得到 `头像.qingbing`，不符合项目一直使用的 `头像.中文名` 可检索规则。
- 已有图片缺少尺寸信息和可选规范化入口，用户无法在注册前判断素材规格。

已修正：

- 头像资源 ID建议顺序改为当前角色显示名、角色 ID、文件名，默认生成 `头像.中文名`；技术文件路径仍独立保存为 `head/qingbing`。
- 所有新头像资源后端入口强制 `头像.名称` 格式，头像选择器和对白角色工具同步做前端校验。
- 头像选择器加载原图后显示真实像素；512×512作为推荐规范，不阻止其他尺寸图片使用或注册。
- 非 512×512 PNG提供可选“备份并规范化为 512×512”，采用等比缩放、透明居中补边，服务端覆盖前保留原图备份。
- 非 PNG明确提示当前自动规范化不支持该格式，但不因尺寸阻止使用。
- `/api/static/speaker`、`/api/static/portrait-resource` 以及通用头像资源创建入口在写入前确认真实图片存在且尺寸元数据可读取，不强制固定像素。
- ES Module 缓存提升为 `20260711-core-17`，Bootstrap 缓存提升为 `20260711-bootstrap-14`，阶段 4样式缓存提升为 `20260711-stage4-8`。

### 2026-07-11：阶段 4 手动验证修正——头像资源创建冲突与可追溯编辑

确认问题：

- “注册对白角色”与头像选择器“创建资源并使用”都会写入 `resources.json`，但旧对白角色端点遇到同名头像资源时会静默复用，没有核对资源组和图片路径。
- 头像选择器点击后立即创建资源，没有创建前摘要、重名提示和二次确认。
- 创建后缺少明确的资源定义编辑入口，资源 ID或 value 填错后用户不知道去哪里修正。

已修正：

- 对白角色后端在任何写入前完成冲突预检：同名角色直接拒绝；同名头像资源只有组为“头像”且 value 完全一致时才允许复用，否则返回明确冲突。
- 预检放在写文件之前，避免发现角色冲突时已经额外创建头像资源。
- 对白角色前端在提交前展示角色 ID、显示名、性别、头像资源 ID、value 和“创建/复用”动作，并要求二次确认。
- 头像选择器实时检查资源 ID是否为空或重名；冲突时禁用创建按钮并显示已有资源提示。
- 创建按钮调整为“检查并确认注册”，确认框完整展示资源 ID、资源组、value 和图片路径，明确资源 ID是稳定引用。
- 资源创建后保持头像选择器打开，立即显示“使用”和“编辑资源定义”操作。
- “编辑资源定义”会定位到高级数据 `resources.json` 的对应定义；当前角色未保存时先要求保存，避免跨文件切换丢失角色修改。
- ES Module 缓存提升为 `20260711-core-17`，Bootstrap 缓存提升为 `20260711-bootstrap-14`，阶段 4样式缓存提升为 `20260711-stage4-8`。

### 2026-07-11：阶段 4用户手动验证通过

- 用户确认角色与伙伴工作区当前功能满足阶段目标，阶段 4正式完成。
- 本阶段手动修正沉淀为“8.7 专用工作区迁移强制规范”，阶段 5之后的资源、地图、物品、装备、商店和剧情工作区必须直接复用。
- 后续发现角色工作区不足时按缺陷或增量需求处理，不再阻塞进入阶段 5。
- 下一阶段为阶段 5“资源管理器与统一资源选择器”。
